# Persist snapshots (RDB)
save 900 1
save 300 10
save 60 10000

# Enable AOF
appendonly yes
appendfilename "appendonly.aof"

# How often AOF is fsynced to disk
appendfsync everysec

# Optional: rewrite AOF to avoid huge file growth
auto-aof-rewrite-percentage 100
auto-aof-rewrite-min-size 64mb